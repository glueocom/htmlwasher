Implement the htmlwasher package (Node.js only).

Read and execute:
1. `prompts/prd.md` — Requirements
2. `prompts/implement.md` — Implementation guide

Key points:
- Generate JSON Schema from sanitize-html types using `ts-json-schema-generator`
- Validate YAML config with Ajv at runtime
- Schema output goes to `dist/schema.json`

Then run tests and fix any problems.
